<% if post.is_delete? %>
  <%= link_to "復元", admin_post_path(@post), method: :patch, class: "btn btn-warning", remote: true %>
<% else %>
  <%= link_to "削除", admin_post_path(@post), method: :patch, class: "btn btn-danger", remote: true %>
<% end %>